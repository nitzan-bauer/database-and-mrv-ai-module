import "server-only";

/**
 * The one real grouped project with a live SaaS marketplace flow and a
 * real product page — every one of Rebeka's 5 scheduled tasks is scoped
 * to it (Nitzan's own spec named this project's product page directly).
 */
export const TARGET_PROJECT_ID = "CARBO-3988";
